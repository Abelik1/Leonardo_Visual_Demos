-r requirements.txt
# Leonardo exposes CUDA 12.2 and NVIDIA A100 (compute capability 8.0).  Keep
# the CuPy wheel on the CUDA 12 family; do not reuse the Windows CUDA 13 lock.
cupy-cuda12x>=13,<15

# PyTorch is optional and is used only by neural_wall.  On Leonardo prefer a
# CINECA `cineca-ai` module whose CUDA build is validated by the site, then
# create the venv with --system-site-packages.  Galaxy collision and the other
# array demos do not require torch.
