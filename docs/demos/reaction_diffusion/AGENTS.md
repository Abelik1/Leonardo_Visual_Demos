# Agent notes: living mathematics

- Preserve the Gray-Scott update and stable parameter ranges.
- Keep total evolution steps independent from the number of output frames.
- Mature reveal members long enough for adjacent feed/kill settings to differ.
- Honour the selected parallel reveal count.
- Keep titles and parameter labels out of the pattern frame.
- If adding drawing input, validate its dimensions and store it with the run.
