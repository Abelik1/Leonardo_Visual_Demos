# Agent notes: AI Plasma Guardian

- Keep all text, sensor bars, loss values, and policy topology out of main frames.
- Amber is the tearing-risk proxy; red is the uncontrolled reference, not a star.
- Keep the baseline outline smooth and visually distinct from the controlled plasma.
- Never label the analytical fallback as neural training.
- Keep policy graph images synchronized with the corresponding main frame.
- Verify controlled/uncontrolled risk values and both CPU and available CUDA paths.
